apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
